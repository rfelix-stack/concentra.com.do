# Directus CMS Configuration
# ================================

# URL base del servidor Directus (accesible desde cliente y servidor)
NUXT_PUBLIC_DIRECTUS_URL=https://admin.concentra.com.do

# Token público de Directus (usado en cliente para lecturas públicas)
DIRECTUS_STATIC_TOKEN=your_public_token_here

# Token privado de Directus (usado en servidor para operaciones con permisos completos)
DIRECTUS_TOKEN=your_private_token_here

# SEO Configuration
# ================================

# URL base del sitio (para canonical URLs y og:url)
NUXT_PUBLIC_SITE_URL=https://concentra.com.do

# Environment
# ================================
NODE_ENV=development

# INSTRUCCIONES:
# ================================
# 1. Copia este archivo a .env en la raíz del proyecto
# 2. Reemplaza los tokens con tus tokens reales de Directus:
#    - DIRECTUS_STATIC_TOKEN: Token con permisos de solo lectura (público)
#    - DIRECTUS_TOKEN: Token con permisos completos (privado, solo servidor)
# 3. NUNCA commites el archivo .env (ya está en .gitignore)
# 4. En producción, estas variables ya están configuradas en el servidor
# 5. Los tokens se generan desde Directus Admin → Settings → Access Tokens
